# Private 3D Prototype

This directory preserves the experimental 3D implementation outside the public
Next.js route tree. It is not approved for public routing, navigation, search, or
static export. Model assets must remain outside `public/` until provenance,
licensing, accessibility, performance, and governance review are complete.
