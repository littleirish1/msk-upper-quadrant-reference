# Duplicate summary

One exact-checksum duplicate group and two probable filename-version groups were identified.

Exact duplicates share one canonical source ID because the bytes and SHA-256 are identical. Probable versions are filename-normalisation candidates only and require manual review. No source was deleted.
