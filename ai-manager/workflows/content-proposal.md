# Content Proposal Workflow

Every proposal records target content ID, reason, existing text, proposed text, supporting source IDs, confidence, limitations, clinical risk, reviewer, decision, and final diff path.

Proposals begin as drafts outside public content. `approved-for-commit` requires a named reviewer, an approved decision, and a reviewed final diff. The schema contains no published state: publication remains a separate repository and clinician-controlled action.
